name: Journalism Dry Run (Walter Croncat)

# Walter Croncat journalism pipeline — DRY RUN ONLY.
#
# This workflow exercises the Stage 1–6 journalism pipeline and writes a
# draft post to drafts/<story_id>_<post_type>.md. It does NOT publish
# anything to X or Bluesky. Publishing is additionally protected by the
# `journalism.enabled: false` master switch in config.yaml.
#
# Triggers:
#   - Manual button in the Actions tab (workflow_dispatch) with optional
#     post-type override
#   - Scheduled run once a day at 13:00 UTC (commented out by default —
#     uncomment when you want continuous draft production)
#
# Outputs the draft and dossier as a downloadable run artifact so you can
# read what the pipeline would have posted without merging anything into
# main.

on:
  workflow_dispatch:
    inputs:
      post_type:
        description: 'Force a post type (leave blank to use brief suggestion)'
        required: false
        default: ''
        type: choice
        options:
          - ''
          - brief
          - meta
          - analysis
          - bulletin
          - primary
      topic:
        description: 'Optional topic to test (leave blank to use trending)'
        required: false
        default: ''
        type: string

  # Uncomment to run a daily dry-run at 13:00 UTC (9 AM ET / 6 AM PT)
  # schedule:
  #   - cron: '0 13 * * *'

permissions:
  contents: write  # needed to commit journalism_seen_stories.txt after each run

jobs:
  dry-run:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - name: Setup environment
        uses: ./.github/actions/setup-python-env

      - name: Run Walter Croncat journalism dry-run
        env:
          # X API credentials — used by Stage 1 trend detection via
          # TwitterBot.client.search_recent_tweets. Falls back to Google
          # News if X is rate-limited or unavailable.
          X_API_KEY: ${{ secrets.X_API_KEY }}
          X_API_SECRET: ${{ secrets.X_API_SECRET }}
          X_ACCESS_TOKEN: ${{ secrets.X_ACCESS_TOKEN }}
          X_ACCESS_TOKEN_SECRET: ${{ secrets.X_ACCESS_TOKEN_SECRET }}
          X_BEARER_TOKEN: ${{ secrets.X_BEARER_TOKEN }}

          # Bluesky credentials — NOT used in dry-run (no publish path),
          # but exported anyway so any incidental import of BlueskyBot
          # does not crash on missing env.
          BLUESKY_USERNAME: ${{ secrets.BLUESKY_USERNAME }}
          BLUESKY_APP_PASSWORD: ${{ secrets.BLUESKY_PASSWORD }}

          # Anthropic: Stage 4 meta-analysis (Opus) + Stage 5 composition (Sonnet)
          ANTHROPIC_API_KEY: ${{ secrets.ANTHROPIC_API_KEY }}

          # Grok: image generation (best-effort in dry-run)
          X_AI_API_KEY: ${{ secrets.X_AI_API_KEY }}

          # Diffbot Article API — Stage 3 body-fetch fallback (free tier, 10K pages/mo)
          DIFFBOT_TOKEN: ${{ secrets.DIFFBOT_TOKEN }}
        run: |
          POST_TYPE="${{ github.event.inputs.post_type }}"
          TOPIC="${{ github.event.inputs.topic }}"
          ARGS="--dry-run"
          if [ -n "$POST_TYPE" ]; then
            ARGS="$POST_TYPE $ARGS"
            echo "Forced post type: $POST_TYPE"
          fi
          if [ -n "$TOPIC" ]; then
            ARGS="$ARGS --topic \"$TOPIC\""
            echo "Manual topic: $TOPIC"
          fi
          echo "Running: python src/main.py journalism $ARGS"
          eval python src/main.py journalism $ARGS

      - name: List drafts and dossiers produced
        if: always()
        run: |
          echo "=== drafts/ ==="
          ls -la drafts/ 2>/dev/null || echo "(no drafts dir)"
          echo ""
          echo "=== dossiers/ ==="
          ls -la dossiers/ 2>/dev/null || echo "(no dossiers dir)"

      - name: Upload drafts and dossiers as artifact
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: journalism-dry-run-${{ github.run_id }}
          path: |
            drafts/
            dossiers/
            docs/dossiers/
          retention-days: 14
          if-no-files-found: warn

      - name: Commit updated seen-stories file (if changed)
        if: success()
        run: |
          # The journalism pipeline appends to journalism_seen_stories.txt
          # after each successfully-composed draft so subsequent cron fires
          # skip stories we've already covered. Commit only if the file
          # actually changed — most runs that fall through on "already seen"
          # or crash before mark-seen will not touch the file.
          git config --local user.email "action@github.com"
          git config --local user.name "GitHub Action"
          if ! git diff --quiet journalism_seen_stories.txt; then
            git add journalism_seen_stories.txt
            git commit -m "Update journalism seen-stories [skip ci]"
            for i in {1..3}; do
              git pull --rebase origin main && git push && break || {
                echo "Push failed (attempt $i/3), retrying..."
                sleep $((i * 2))
              }
            done
          else
            echo "No changes to journalism_seen_stories.txt, skipping commit"
          fi

      - name: Commit dossier viewer pages (if generated)
        if: success()
        run: |
          git config --local user.email "action@github.com"
          git config --local user.name "GitHub Action"
          git add docs/dossiers/ docs/CNAME docs/sitemap.xml docs/robots.txt 2>/dev/null || true
          if ! git diff --staged --quiet; then
            git commit -m "Update dossier viewer pages [skip ci]"
            for i in {1..3}; do
              git pull --rebase origin main && git push && break || {
                echo "Push failed (attempt $i/3), retrying..."
                sleep $((i * 2))
              }
            done
          else
            echo "No dossier page changes to commit"
          fi
